import { UserItem } from "components";
import React from "react";

const Home = () => {
  return (
    <main>
      <section>
        <UserItem />
      </section>
    </main>
  );
};

export default Home;
